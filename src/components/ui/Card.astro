---
export interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class={`rounded-lg bg-white shadow-card transition-shadow hover:shadow-card-hover ${className ?? ''}`}>
  <slot />
</div>
